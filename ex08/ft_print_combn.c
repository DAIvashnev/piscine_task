void ft_putchar(char c) {
    write(1, &c, 1);
}

// это задание можешь считать как задачку со звездочкой. 
// Тут скорее всего стоило ввести массив на 10 элиментов и на него ориентироваться. 

void ft_print_combn(int n) {
    if(n>0 || n<10) {

        //тут даже мыслей не было, как это решать. Так как у нас колличество цифр зависит от веденного n. 
        //Под n=2, еще могу создат ьдве переменные и просчитать, но дальше хз бро.
    }
}